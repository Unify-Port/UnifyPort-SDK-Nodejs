<!-- Generated by pnpm generate. Update the public contract or generator instead of editing this file. -->

# Webhook Endpoints

[English](webhook-endpoints.md) | [简体中文](../zh-CN/api-reference/webhook-endpoints.md)

[Back to the API Reference index](README.md)

Application webhook endpoint management.

## Operations on this page

- [`createWebhookEndpoint`](#create-webhook-endpoint): Create a webhook endpoint
- [`deactivateWebhookEndpoint`](#deactivate-webhook-endpoint): Disable a webhook endpoint
- [`deleteWebhookEndpoint`](#delete-webhook-endpoint): Delete a webhook endpoint
- [`getWebhookEndpoint`](#get-webhook-endpoint): Get a webhook endpoint
- [`listWebhookEndpoints`](#list-webhook-endpoints): List webhook endpoints
- [`updateWebhookEndpoint`](#update-webhook-endpoint): Update a webhook endpoint

---

<a id="create-webhook-endpoint"></a>

## `createWebhookEndpoint`

Create a webhook endpoint

| Property        | Value                                                                             |
| --------------- | --------------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/webhook-endpoints`                                                    |
| SDK method      | `device.createWebhookEndpoint(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["createWebhookEndpoint"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                              |
| Mutability      | `write`                                                                           |
| Automatic retry | No automatic retries                                                              |

### Parameters

No path, query, header, or cookie parameters.

### Request body

JSON request body.

| Field                    | Required | Type                       | Constraints | Description                                                                                                      |
| ------------------------ | -------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| `body.url`               | Yes      | `string (uri)`             | -           | -                                                                                                                |
| `body.status`            | Yes      | `string`                   | -           | -                                                                                                                |
| `body.subscribed_events` | No       | `Array<StandardEventType>` | -           | An empty or omitted value subscribes to all events.                                                              |
| `body.signing_secret`    | No       | `string`                   | -           | The secret used to sign webhook deliveries. It is submitted only in requests and is never returned in responses. |
| `body.retry_policy`      | No       | `FreeFormObject`           | -           | -                                                                                                                |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                    |
| ----------- | ------------------ | ----------------------- | ---------------------------------------------- |
| `201`       | `application/json` | `ResponseMeta & object` | Created successfully.                          |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.    |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `201` response fields:

| Field                                | Present | Type                       | Constraints | Description                                                            |
| ------------------------------------ | ------- | -------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`             | No      | `string`                   | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`      | No      | `string`                   | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                   | Yes     | `WebhookEndpoint`          | -           | -                                                                      |
| `result.data.data.id`                | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.url`               | Yes     | `string (uri)`             | -           | -                                                                      |
| `result.data.data.status`            | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.subscribed_events` | No      | `Array<StandardEventType>` | -           | -                                                                      |
| `result.data.data.signing_enabled`   | Yes     | `boolean`                  | -           | -                                                                      |
| `result.data.data.retry_policy`      | No      | `FreeFormObject`           | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.createWebhookEndpoint({
  body: {
    url: "https://example.com/resource",
    status: "active"
  }
});
console.log(result.data.data);
```

> Sensitive values in this example are placeholders. Read real values from a controlled runtime environment and never write them to logs.

---

<a id="deactivate-webhook-endpoint"></a>

## `deactivateWebhookEndpoint`

Disable a webhook endpoint

| Property        | Value                                                                                 |
| --------------- | ------------------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/webhook-endpoints/{endpoint_id}/deactivate`                               |
| SDK method      | `device.deactivateWebhookEndpoint(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["deactivateWebhookEndpoint"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                                  |
| Mutability      | `write`                                                                               |
| Automatic retry | No automatic retries                                                                  |

### Parameters

| SDK field                 | Location | Required | Type     | Example    | Constraints | Description                                             |
| ------------------------- | -------- | -------- | -------- | ---------- | ----------- | ------------------------------------------------------- |
| `params.path.endpoint_id` | path     | Yes      | `string` | `"we_xxx"` | -           | The webhook endpoint `public_id`, for example `we_xxx`. |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                    |
| ----------- | ------------------ | ----------------------- | ---------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The disabled webhook endpoint.                 |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.    |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                | Present | Type                       | Constraints | Description                                                            |
| ------------------------------------ | ------- | -------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`             | No      | `string`                   | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`      | No      | `string`                   | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                   | Yes     | `WebhookEndpoint`          | -           | -                                                                      |
| `result.data.data.id`                | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.url`               | Yes     | `string (uri)`             | -           | -                                                                      |
| `result.data.data.status`            | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.subscribed_events` | No      | `Array<StandardEventType>` | -           | -                                                                      |
| `result.data.data.signing_enabled`   | Yes     | `boolean`                  | -           | -                                                                      |
| `result.data.data.retry_policy`      | No      | `FreeFormObject`           | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.deactivateWebhookEndpoint({
  params: {
    path: {
      endpoint_id: "we_xxx"
    }
  }
});
console.log(result.data.data);
```

---

<a id="delete-webhook-endpoint"></a>

## `deleteWebhookEndpoint`

Delete a webhook endpoint

| Property        | Value                                                                             |
| --------------- | --------------------------------------------------------------------------------- |
| HTTP            | `DELETE` `/v1/webhook-endpoints/{endpoint_id}`                                    |
| SDK method      | `device.deleteWebhookEndpoint(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["deleteWebhookEndpoint"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                              |
| Mutability      | `destructive`                                                                     |
| Automatic retry | No automatic retries                                                              |

### Parameters

| SDK field                 | Location | Required | Type     | Example    | Constraints | Description                                             |
| ------------------------- | -------- | -------- | -------- | ---------- | ----------- | ------------------------------------------------------- |
| `params.path.endpoint_id` | path     | Yes      | `string` | `"we_xxx"` | -           | The webhook endpoint `public_id`, for example `we_xxx`. |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema          | Description                                     |
| ----------- | ------------------ | --------------- | ----------------------------------------------- |
| `204`       | None               | -               | Deleted successfully. The response has no body. |
| `400`       | `application/json` | `ErrorEnvelope` | The request body or parameters are invalid.     |
| `401`       | `application/json` | `ErrorEnvelope` | The API key is missing or cannot be validated.  |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `204` has no response body; `result.data` is `undefined`.

### TypeScript example

```ts
const result = await device.deleteWebhookEndpoint({
  params: {
    path: {
      endpoint_id: "we_xxx"
    }
  }
});
console.log(result.status, result.requestId);
```

---

<a id="get-webhook-endpoint"></a>

## `getWebhookEndpoint`

Get a webhook endpoint

| Property        | Value                                                                          |
| --------------- | ------------------------------------------------------------------------------ |
| HTTP            | `GET` `/v1/webhook-endpoints/{endpoint_id}`                                    |
| SDK method      | `device.getWebhookEndpoint(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["getWebhookEndpoint"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                           |
| Mutability      | `read`                                                                         |
| Automatic retry | Safe retries enabled                                                           |

### Parameters

| SDK field                 | Location | Required | Type     | Example    | Constraints | Description                                             |
| ------------------------- | -------- | -------- | -------- | ---------- | ----------- | ------------------------------------------------------- |
| `params.path.endpoint_id` | path     | Yes      | `string` | `"we_xxx"` | -           | The webhook endpoint `public_id`, for example `we_xxx`. |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | Webhook endpoint details.                                                  |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                | Present | Type                       | Constraints | Description                                                            |
| ------------------------------------ | ------- | -------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`             | No      | `string`                   | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`      | No      | `string`                   | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                   | Yes     | `WebhookEndpoint`          | -           | -                                                                      |
| `result.data.data.id`                | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.url`               | Yes     | `string (uri)`             | -           | -                                                                      |
| `result.data.data.status`            | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.subscribed_events` | No      | `Array<StandardEventType>` | -           | -                                                                      |
| `result.data.data.signing_enabled`   | Yes     | `boolean`                  | -           | -                                                                      |
| `result.data.data.retry_policy`      | No      | `FreeFormObject`           | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.getWebhookEndpoint({
  params: {
    path: {
      endpoint_id: "we_xxx"
    }
  }
});
console.log(result.data.data);
```

---

<a id="list-webhook-endpoints"></a>

## `listWebhookEndpoints`

List webhook endpoints

| Property        | Value                                                                            |
| --------------- | -------------------------------------------------------------------------------- |
| HTTP            | `GET` `/v1/webhook-endpoints`                                                    |
| SDK method      | `device.listWebhookEndpoints(request?, execution?)`                              |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["listWebhookEndpoints"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                             |
| Mutability      | `read`                                                                           |
| Automatic retry | Safe retries enabled                                                             |

### Parameters

No path, query, header, or cookie parameters.

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | A list of webhook endpoints.                                               |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                  | Present | Type                       | Constraints | Description                                                            |
| -------------------------------------- | ------- | -------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`               | No      | `string`                   | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`        | No      | `string`                   | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                     | Yes     | `Array<WebhookEndpoint>`   | -           | -                                                                      |
| `result.data.data[].id`                | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data[].url`               | Yes     | `string (uri)`             | -           | -                                                                      |
| `result.data.data[].status`            | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data[].subscribed_events` | No      | `Array<StandardEventType>` | -           | -                                                                      |
| `result.data.data[].signing_enabled`   | Yes     | `boolean`                  | -           | -                                                                      |
| `result.data.data[].retry_policy`      | No      | `FreeFormObject`           | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.listWebhookEndpoints();
console.log(result.data.data);
```

---

<a id="update-webhook-endpoint"></a>

## `updateWebhookEndpoint`

Update a webhook endpoint

| Property        | Value                                                                             |
| --------------- | --------------------------------------------------------------------------------- |
| HTTP            | `PATCH` `/v1/webhook-endpoints/{endpoint_id}`                                     |
| SDK method      | `device.updateWebhookEndpoint(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["updateWebhookEndpoint"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                              |
| Mutability      | `write`                                                                           |
| Automatic retry | No automatic retries                                                              |

### Parameters

| SDK field                 | Location | Required | Type     | Example    | Constraints | Description                                             |
| ------------------------- | -------- | -------- | -------- | ---------- | ----------- | ------------------------------------------------------- |
| `params.path.endpoint_id` | path     | Yes      | `string` | `"we_xxx"` | -           | The webhook endpoint `public_id`, for example `we_xxx`. |

### Request body

JSON request body.

| Field                    | Required | Type                       | Constraints | Description                                                                                                      |
| ------------------------ | -------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| `body.url`               | Yes      | `string (uri)`             | -           | -                                                                                                                |
| `body.status`            | Yes      | `string`                   | -           | -                                                                                                                |
| `body.subscribed_events` | No       | `Array<StandardEventType>` | -           | An empty or omitted value subscribes to all events.                                                              |
| `body.signing_secret`    | No       | `string`                   | -           | The secret used to sign webhook deliveries. It is submitted only in requests and is never returned in responses. |
| `body.retry_policy`      | No       | `FreeFormObject`           | -           | -                                                                                                                |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                    |
| ----------- | ------------------ | ----------------------- | ---------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The updated webhook endpoint.                  |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.    |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                | Present | Type                       | Constraints | Description                                                            |
| ------------------------------------ | ------- | -------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`             | No      | `string`                   | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`      | No      | `string`                   | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                   | Yes     | `WebhookEndpoint`          | -           | -                                                                      |
| `result.data.data.id`                | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.url`               | Yes     | `string (uri)`             | -           | -                                                                      |
| `result.data.data.status`            | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.subscribed_events` | No      | `Array<StandardEventType>` | -           | -                                                                      |
| `result.data.data.signing_enabled`   | Yes     | `boolean`                  | -           | -                                                                      |
| `result.data.data.retry_policy`      | No      | `FreeFormObject`           | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.updateWebhookEndpoint({
  params: {
    path: {
      endpoint_id: "we_xxx"
    }
  },
  body: {
    url: "https://example.com/resource",
    status: "active"
  }
});
console.log(result.data.data);
```

> Sensitive values in this example are placeholders. Read real values from a controlled runtime environment and never write them to logs.
