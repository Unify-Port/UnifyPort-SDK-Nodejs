<!-- Generated by pnpm generate. Update the public contract or generator instead of editing this file. -->

# Account Runtime

[English](account-runtime.md) | [简体中文](../zh-CN/api-reference/account-runtime.md)

[Back to the API Reference index](README.md)

Provider account runtime controls.

## Operations on this page

- [`reconnectAccountRuntime`](#reconnect-account-runtime): Reconnect account runtime
- [`refreshAccountRuntime`](#refresh-account-runtime): Refresh account runtime state
- [`startAccountRuntime`](#start-account-runtime): Start account runtime
- [`stopAccountRuntime`](#stop-account-runtime): Stop account runtime

---

<a id="reconnect-account-runtime"></a>

## `reconnectAccountRuntime`

Reconnect account runtime

| Property        | Value                                                                               |
| --------------- | ----------------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts/{account_id}/runtime/reconnect`                                |
| SDK method      | `device.reconnectAccountRuntime(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["reconnectAccountRuntime"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                                |
| Mutability      | `write`                                                                             |
| Automatic retry | No automatic retries                                                                |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

Provider action parameters. Supported fields may vary by provider; an empty object means no additional parameters are sent.

| Field  | Required | Type             | Constraints | Description |
| ------ | -------- | ---------------- | ----------- | ----------- |
| `body` | No       | `FreeFormObject` | -           | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The account runtime action result.                                         |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                               | Present | Type                                                                | Constraints                                                                                           | Description                                                                                |
| ----------------------------------- | ------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `result.data.request_id`            | No      | `string`                                                            | -                                                                                                     | The request ID generated by the server.                                                    |
| `result.data.client_request_id`     | No      | `string`                                                            | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                     |
| `result.data.data`                  | Yes     | `RuntimeActionResult`                                               | -                                                                                                     | -                                                                                          |
| `result.data.data.account_id`       | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.provider`         | Yes     | `ProviderName`                                                      | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok", "whatsapp_protocol"            | The provider name. Actual availability depends on the capabilities of the current account. |
| `result.data.data.action`           | Yes     | `string enum("refresh_status" \| "start" \| "stop" \| "reconnect")` | enum="refresh_status", "start", "stop", "reconnect"                                                   | -                                                                                          |
| `result.data.data.operation_status` | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.runtime_status`   | Yes     | `RuntimeStatus`                                                     | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                          |
| `result.data.data.runtime_error`    | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |

### TypeScript example

```ts
const result = await device.reconnectAccountRuntime({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  }
});
console.log(result.data.data);
```

---

<a id="refresh-account-runtime"></a>

## `refreshAccountRuntime`

Refresh account runtime state

| Property        | Value                                                                             |
| --------------- | --------------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts/{account_id}/runtime/refresh`                                |
| SDK method      | `device.refreshAccountRuntime(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["refreshAccountRuntime"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                              |
| Mutability      | `write`                                                                           |
| Automatic retry | No automatic retries                                                              |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

Provider action parameters. Supported fields may vary by provider; an empty object means no additional parameters are sent.

| Field  | Required | Type             | Constraints | Description |
| ------ | -------- | ---------------- | ----------- | ----------- |
| `body` | No       | `FreeFormObject` | -           | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The account runtime action result.                                         |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                               | Present | Type                                                                | Constraints                                                                                           | Description                                                                                |
| ----------------------------------- | ------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `result.data.request_id`            | No      | `string`                                                            | -                                                                                                     | The request ID generated by the server.                                                    |
| `result.data.client_request_id`     | No      | `string`                                                            | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                     |
| `result.data.data`                  | Yes     | `RuntimeActionResult`                                               | -                                                                                                     | -                                                                                          |
| `result.data.data.account_id`       | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.provider`         | Yes     | `ProviderName`                                                      | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok", "whatsapp_protocol"            | The provider name. Actual availability depends on the capabilities of the current account. |
| `result.data.data.action`           | Yes     | `string enum("refresh_status" \| "start" \| "stop" \| "reconnect")` | enum="refresh_status", "start", "stop", "reconnect"                                                   | -                                                                                          |
| `result.data.data.operation_status` | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.runtime_status`   | Yes     | `RuntimeStatus`                                                     | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                          |
| `result.data.data.runtime_error`    | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |

### TypeScript example

```ts
const result = await device.refreshAccountRuntime({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  }
});
console.log(result.data.data);
```

---

<a id="start-account-runtime"></a>

## `startAccountRuntime`

Start account runtime

| Property        | Value                                                                           |
| --------------- | ------------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts/{account_id}/runtime/start`                                |
| SDK method      | `device.startAccountRuntime(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["startAccountRuntime"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                            |
| Mutability      | `write`                                                                         |
| Automatic retry | No automatic retries                                                            |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

Provider action parameters. Supported fields may vary by provider; an empty object means no additional parameters are sent.

| Field  | Required | Type             | Constraints | Description |
| ------ | -------- | ---------------- | ----------- | ----------- |
| `body` | No       | `FreeFormObject` | -           | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The account runtime action result.                                         |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                               | Present | Type                                                                | Constraints                                                                                           | Description                                                                                |
| ----------------------------------- | ------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `result.data.request_id`            | No      | `string`                                                            | -                                                                                                     | The request ID generated by the server.                                                    |
| `result.data.client_request_id`     | No      | `string`                                                            | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                     |
| `result.data.data`                  | Yes     | `RuntimeActionResult`                                               | -                                                                                                     | -                                                                                          |
| `result.data.data.account_id`       | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.provider`         | Yes     | `ProviderName`                                                      | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok", "whatsapp_protocol"            | The provider name. Actual availability depends on the capabilities of the current account. |
| `result.data.data.action`           | Yes     | `string enum("refresh_status" \| "start" \| "stop" \| "reconnect")` | enum="refresh_status", "start", "stop", "reconnect"                                                   | -                                                                                          |
| `result.data.data.operation_status` | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.runtime_status`   | Yes     | `RuntimeStatus`                                                     | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                          |
| `result.data.data.runtime_error`    | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |

### TypeScript example

```ts
const result = await device.startAccountRuntime({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  }
});
console.log(result.data.data);
```

---

<a id="stop-account-runtime"></a>

## `stopAccountRuntime`

Stop account runtime

| Property        | Value                                                                          |
| --------------- | ------------------------------------------------------------------------------ |
| HTTP            | `POST` `/v1/accounts/{account_id}/runtime/stop`                                |
| SDK method      | `device.stopAccountRuntime(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["stopAccountRuntime"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                           |
| Mutability      | `write`                                                                        |
| Automatic retry | No automatic retries                                                           |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

Provider action parameters. Supported fields may vary by provider; an empty object means no additional parameters are sent.

| Field  | Required | Type             | Constraints | Description |
| ------ | -------- | ---------------- | ----------- | ----------- |
| `body` | No       | `FreeFormObject` | -           | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The account runtime action result.                                         |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                               | Present | Type                                                                | Constraints                                                                                           | Description                                                                                |
| ----------------------------------- | ------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `result.data.request_id`            | No      | `string`                                                            | -                                                                                                     | The request ID generated by the server.                                                    |
| `result.data.client_request_id`     | No      | `string`                                                            | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                     |
| `result.data.data`                  | Yes     | `RuntimeActionResult`                                               | -                                                                                                     | -                                                                                          |
| `result.data.data.account_id`       | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.provider`         | Yes     | `ProviderName`                                                      | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok", "whatsapp_protocol"            | The provider name. Actual availability depends on the capabilities of the current account. |
| `result.data.data.action`           | Yes     | `string enum("refresh_status" \| "start" \| "stop" \| "reconnect")` | enum="refresh_status", "start", "stop", "reconnect"                                                   | -                                                                                          |
| `result.data.data.operation_status` | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |
| `result.data.data.runtime_status`   | Yes     | `RuntimeStatus`                                                     | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                          |
| `result.data.data.runtime_error`    | Yes     | `string`                                                            | -                                                                                                     | -                                                                                          |

### TypeScript example

```ts
const result = await device.stopAccountRuntime({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  }
});
console.log(result.data.data);
```
