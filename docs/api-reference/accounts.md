<!-- Generated by pnpm generate. Update the public contract or generator instead of editing this file. -->

# Accounts

[English](accounts.md) | [简体中文](../zh-CN/api-reference/accounts.md)

[Back to the API Reference index](README.md)

Provider account resource management.

## Operations on this page

- [`createAccount`](#create-account): Create an account
- [`deleteAccount`](#delete-account): Delete an account
- [`getAccount`](#get-account): Get account details
- [`listAccounts`](#list-accounts): List accounts
- [`updateAccount`](#update-account): Update an account

---

<a id="create-account"></a>

## `createAccount`

Create an account

| Property        | Value                                                                     |
| --------------- | ------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts`                                                     |
| SDK method      | `device.createAccount(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["createAccount"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                      |
| Mutability      | `write`                                                                   |
| Automatic retry | No automatic retries                                                      |

### Parameters

No path, query, header, or cookie parameters.

### Request body

JSON request body.

| Field                       | Required | Type             | Constraints                                                                                           | Description                                                                                                           |
| --------------------------- | -------- | ---------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `body.name`                 | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.provider`             | Yes      | `ProviderName`   | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok"                                 | Publicly available provider name. Use the provider regions endpoint to determine where new accounts can be allocated. |
| `body.region`               | Yes      | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.status`               | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.runtime_status`       | No       | `RuntimeStatus`  | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                                                     |
| `body.auth_mode`            | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.capabilities`         | No       | `Array<string>`  | -                                                                                                     | -                                                                                                                     |
| `body.metadata`             | No       | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `body.provider_account_ref` | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.provider_data`        | No       | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `body.proxy`                | No       | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `201`       | `application/json` | `ResponseMeta & object` | Created successfully.                                                      |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `503`       | `application/json` | `ErrorEnvelope`         | The service is temporarily unavailable.                                    |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `201` response fields:

| Field                                   | Present | Type             | Constraints                                                                                           | Description                                                                                                           |
| --------------------------------------- | ------- | ---------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `result.data.request_id`                | No      | `string`         | -                                                                                                     | The request ID generated by the server.                                                                               |
| `result.data.client_request_id`         | No      | `string`         | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                                                |
| `result.data.data`                      | Yes     | `Account`        | -                                                                                                     | -                                                                                                                     |
| `result.data.data.id`                   | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.name`                 | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider`             | Yes     | `ProviderName`   | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok"                                 | Publicly available provider name. Use the provider regions endpoint to determine where new accounts can be allocated. |
| `result.data.data.region`               | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.status`               | Yes     | `string`         | -                                                                                                     | The account resource status, for example `active`, `inactive`, or `disabled`.                                         |
| `result.data.data.runtime_status`       | No      | `RuntimeStatus`  | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                                                     |
| `result.data.data.auth_mode`            | No      | `string`         | -                                                                                                     | The authorization mode, for example `qrcode`, `code`, or `session`.                                                   |
| `result.data.data.capabilities`         | No      | `Array<string>`  | -                                                                                                     | -                                                                                                                     |
| `result.data.data.metadata`             | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider_account_ref` | No      | `string`         | -                                                                                                     | The provider-side public account identifier.                                                                          |
| `result.data.data.proxy`                | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider_profile`     | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |

### TypeScript example

```ts
const result = await device.createAccount({
  body: {
    provider: "telegram",
    region: "global"
  }
});
console.log(result.data.data);
```

> Sensitive values in this example are placeholders. Read real values from a controlled runtime environment and never write them to logs.

---

<a id="delete-account"></a>

## `deleteAccount`

Delete an account

| Property        | Value                                                                     |
| --------------- | ------------------------------------------------------------------------- |
| HTTP            | `DELETE` `/v1/accounts/{account_id}`                                      |
| SDK method      | `device.deleteAccount(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["deleteAccount"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                      |
| Mutability      | `destructive`                                                             |
| Automatic retry | No automatic retries                                                      |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema          | Description                                                                |
| ----------- | ------------------ | --------------- | -------------------------------------------------------------------------- |
| `204`       | None               | -               | Deleted successfully. The response has no body.                            |
| `401`       | `application/json` | `ErrorEnvelope` | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope` | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope` | An internal server error or an unmapped error in the provider integration. |
| `502`       | `application/json` | `ErrorEnvelope` | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `204` has no response body; `result.data` is `undefined`.

### TypeScript example

```ts
const result = await device.deleteAccount({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  }
});
console.log(result.status, result.requestId);
```

---

<a id="get-account"></a>

## `getAccount`

Get account details

| Property        | Value                                                                  |
| --------------- | ---------------------------------------------------------------------- |
| HTTP            | `GET` `/v1/accounts/{account_id}`                                      |
| SDK method      | `device.getAccount(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["getAccount"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                   |
| Mutability      | `read`                                                                 |
| Automatic retry | Safe retries enabled                                                   |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | Account details.                                                           |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                   | Present | Type             | Constraints                                                                                           | Description                                                                                                           |
| --------------------------------------- | ------- | ---------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `result.data.request_id`                | No      | `string`         | -                                                                                                     | The request ID generated by the server.                                                                               |
| `result.data.client_request_id`         | No      | `string`         | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                                                |
| `result.data.data`                      | Yes     | `Account`        | -                                                                                                     | -                                                                                                                     |
| `result.data.data.id`                   | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.name`                 | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider`             | Yes     | `ProviderName`   | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok"                                 | Publicly available provider name. Use the provider regions endpoint to determine where new accounts can be allocated. |
| `result.data.data.region`               | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.status`               | Yes     | `string`         | -                                                                                                     | The account resource status, for example `active`, `inactive`, or `disabled`.                                         |
| `result.data.data.runtime_status`       | No      | `RuntimeStatus`  | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                                                     |
| `result.data.data.auth_mode`            | No      | `string`         | -                                                                                                     | The authorization mode, for example `qrcode`, `code`, or `session`.                                                   |
| `result.data.data.capabilities`         | No      | `Array<string>`  | -                                                                                                     | -                                                                                                                     |
| `result.data.data.metadata`             | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider_account_ref` | No      | `string`         | -                                                                                                     | The provider-side public account identifier.                                                                          |
| `result.data.data.proxy`                | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider_profile`     | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |

> This operation may return one-time or sensitive data. Handle it only in controlled locations and never log the full response.

### TypeScript example

```ts
const result = await device.getAccount({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  }
});
console.log(result.status, result.requestId);
```

---

<a id="list-accounts"></a>

## `listAccounts`

List accounts

| Property        | Value                                                                    |
| --------------- | ------------------------------------------------------------------------ |
| HTTP            | `GET` `/v1/accounts`                                                     |
| SDK method      | `device.listAccounts(request?, execution?)`                              |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["listAccounts"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                     |
| Mutability      | `read`                                                                   |
| Automatic retry | Safe retries enabled                                                     |

### Parameters

No path, query, header, or cookie parameters.

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | A list of accounts in the current workspace.                               |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                     | Present | Type             | Constraints                                                                                           | Description                                                                                                           |
| ----------------------------------------- | ------- | ---------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `result.data.request_id`                  | No      | `string`         | -                                                                                                     | The request ID generated by the server.                                                                               |
| `result.data.client_request_id`           | No      | `string`         | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                                                |
| `result.data.data`                        | Yes     | `Array<Account>` | -                                                                                                     | -                                                                                                                     |
| `result.data.data[].id`                   | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data[].name`                 | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data[].provider`             | Yes     | `ProviderName`   | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok"                                 | Publicly available provider name. Use the provider regions endpoint to determine where new accounts can be allocated. |
| `result.data.data[].region`               | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data[].status`               | Yes     | `string`         | -                                                                                                     | The account resource status, for example `active`, `inactive`, or `disabled`.                                         |
| `result.data.data[].runtime_status`       | No      | `RuntimeStatus`  | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                                                     |
| `result.data.data[].auth_mode`            | No      | `string`         | -                                                                                                     | The authorization mode, for example `qrcode`, `code`, or `session`.                                                   |
| `result.data.data[].capabilities`         | No      | `Array<string>`  | -                                                                                                     | -                                                                                                                     |
| `result.data.data[].metadata`             | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data[].provider_account_ref` | No      | `string`         | -                                                                                                     | The provider-side public account identifier.                                                                          |
| `result.data.data[].proxy`                | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data[].provider_profile`     | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |

### TypeScript example

```ts
const result = await device.listAccounts();
console.log(result.data.data);
```

---

<a id="update-account"></a>

## `updateAccount`

Update an account

| Property        | Value                                                                     |
| --------------- | ------------------------------------------------------------------------- |
| HTTP            | `PATCH` `/v1/accounts/{account_id}`                                       |
| SDK method      | `device.updateAccount(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["updateAccount"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                      |
| Mutability      | `write`                                                                   |
| Automatic retry | No automatic retries                                                      |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

JSON request body.

| Field                       | Required | Type             | Constraints                                                                                           | Description                                                                                                           |
| --------------------------- | -------- | ---------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `body.name`                 | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.provider`             | Yes      | `ProviderName`   | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok"                                 | Publicly available provider name. Use the provider regions endpoint to determine where new accounts can be allocated. |
| `body.region`               | Yes      | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.status`               | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.runtime_status`       | No       | `RuntimeStatus`  | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                                                     |
| `body.auth_mode`            | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.capabilities`         | No       | `Array<string>`  | -                                                                                                     | -                                                                                                                     |
| `body.metadata`             | No       | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `body.provider_account_ref` | No       | `string`         | -                                                                                                     | -                                                                                                                     |
| `body.provider_data`        | No       | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `body.proxy`                | No       | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The updated account.                                                       |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                   | Present | Type             | Constraints                                                                                           | Description                                                                                                           |
| --------------------------------------- | ------- | ---------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `result.data.request_id`                | No      | `string`         | -                                                                                                     | The request ID generated by the server.                                                                               |
| `result.data.client_request_id`         | No      | `string`         | -                                                                                                     | Echoed when the client supplies a valid `X-Request-Id` request header.                                                |
| `result.data.data`                      | Yes     | `Account`        | -                                                                                                     | -                                                                                                                     |
| `result.data.data.id`                   | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.name`                 | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider`             | Yes     | `ProviderName`   | enum="telegram", "whatsapp", "line", "twitter", "x", "zalo", "tiktok"                                 | Publicly available provider name. Use the provider regions endpoint to determine where new accounts can be allocated. |
| `result.data.data.region`               | Yes     | `string`         | -                                                                                                     | -                                                                                                                     |
| `result.data.data.status`               | Yes     | `string`         | -                                                                                                     | The account resource status, for example `active`, `inactive`, or `disabled`.                                         |
| `result.data.data.runtime_status`       | No      | `RuntimeStatus`  | enum="unknown", "starting", "running", "stopping", "stopped", "reconnecting", "disconnected", "error" | -                                                                                                                     |
| `result.data.data.auth_mode`            | No      | `string`         | -                                                                                                     | The authorization mode, for example `qrcode`, `code`, or `session`.                                                   |
| `result.data.data.capabilities`         | No      | `Array<string>`  | -                                                                                                     | -                                                                                                                     |
| `result.data.data.metadata`             | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider_account_ref` | No      | `string`         | -                                                                                                     | The provider-side public account identifier.                                                                          |
| `result.data.data.proxy`                | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |
| `result.data.data.provider_profile`     | No      | `FreeFormObject` | -                                                                                                     | -                                                                                                                     |

### TypeScript example

```ts
const result = await device.updateAccount({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  },
  body: {
    provider: "telegram",
    region: "global"
  }
});
console.log(result.data.data);
```

> Sensitive values in this example are placeholders. Read real values from a controlled runtime environment and never write them to logs.
