<!-- Generated by pnpm generate. Update the public contract or generator instead of editing this file. -->

# Workspace

[English](workspace.md) | [简体中文](../zh-CN/api-reference/workspace.md)

[Back to the API Reference index](README.md)

The workspace associated with the current API key.

## Operations on this page

- [`getWorkspace`](#get-workspace): Get the current workspace
- [`updateWorkspace`](#update-workspace): Update the current workspace

---

<a id="get-workspace"></a>

## `getWorkspace`

Get the current workspace

| Property        | Value                                                                    |
| --------------- | ------------------------------------------------------------------------ |
| HTTP            | `GET` `/v1/workspace`                                                    |
| SDK method      | `device.getWorkspace(request?, execution?)`                              |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["getWorkspace"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                     |
| Mutability      | `read`                                                                   |
| Automatic retry | Safe retries enabled                                                     |

### Parameters

No path, query, header, or cookie parameters.

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | Information about the current workspace.                                   |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                      | Present | Type                        | Constraints | Description                                                            |
| ------------------------------------------ | ------- | --------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`                   | No      | `string`                    | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`            | No      | `string`                    | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                         | Yes     | `Workspace`                 | -           | -                                                                      |
| `result.data.data.name`                    | Yes     | `string`                    | -           | -                                                                      |
| `result.data.data.status`                  | Yes     | `string`                    | -           | -                                                                      |
| `result.data.data.metadata`                | No      | `Record<string, string>`    | -           | -                                                                      |
| `result.data.data.account_quota`           | Yes     | `number \| string (uint64)` | -           | -                                                                      |
| `result.data.data.account_quota_unlimited` | Yes     | `boolean`                   | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.getWorkspace();
console.log(result.data.data);
```

---

<a id="update-workspace"></a>

## `updateWorkspace`

Update the current workspace

| Property        | Value                                                                       |
| --------------- | --------------------------------------------------------------------------- |
| HTTP            | `PATCH` `/v1/workspace`                                                     |
| SDK method      | `device.updateWorkspace(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["updateWorkspace"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                        |
| Mutability      | `write`                                                                     |
| Automatic retry | No automatic retries                                                        |

### Parameters

No path, query, header, or cookie parameters.

### Request body

JSON request body.

| Field           | Required | Type                     | Constraints | Description |
| --------------- | -------- | ------------------------ | ----------- | ----------- |
| `body.name`     | Yes      | `string`                 | -           | -           |
| `body.status`   | Yes      | `string`                 | -           | -           |
| `body.metadata` | No       | `Record<string, string>` | -           | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | Information about the updated workspace.                                   |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                      | Present | Type                        | Constraints | Description                                                            |
| ------------------------------------------ | ------- | --------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`                   | No      | `string`                    | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`            | No      | `string`                    | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                         | Yes     | `Workspace`                 | -           | -                                                                      |
| `result.data.data.name`                    | Yes     | `string`                    | -           | -                                                                      |
| `result.data.data.status`                  | Yes     | `string`                    | -           | -                                                                      |
| `result.data.data.metadata`                | No      | `Record<string, string>`    | -           | -                                                                      |
| `result.data.data.account_quota`           | Yes     | `number \| string (uint64)` | -           | -                                                                      |
| `result.data.data.account_quota_unlimited` | Yes     | `boolean`                   | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.updateWorkspace({
  body: {
    name: "Example",
    status: "active"
  }
});
console.log(result.data.data);
```
