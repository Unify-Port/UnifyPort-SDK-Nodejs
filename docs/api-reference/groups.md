<!-- Generated by pnpm generate. Update the public contract or generator instead of editing this file. -->

# Groups

[English](groups.md) | [简体中文](../zh-CN/api-reference/groups.md)

[Back to the API Reference index](README.md)

Provider group views and group actions.

## Operations on this page

- [`createGroup`](#create-group): Create a group
- [`getGroup`](#get-group): Get group details
- [`getGroupInviteCode`](#get-group-invite-code): Get a group invitation link or code
- [`leaveGroup`](#leave-group): Leave a group
- [`listGroupJoinRequests`](#list-group-join-requests): List pending group join requests
- [`listGroups`](#list-groups): List groups
- [`setGroupJoinApprovalMode`](#set-group-join-approval-mode): Set the group join approval mode
- [`updateGroupInfo`](#update-group-info): Update group information
- [`updateGroupJoinRequests`](#update-group-join-requests): Process a group join request
- [`updateGroupMembers`](#update-group-members): Manage group members

---

<a id="create-group"></a>

## `createGroup`

Create a group

| Property        | Value                                                                   |
| --------------- | ----------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts/{account_id}/groups/create`                        |
| SDK method      | `device.createGroup(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["createGroup"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                    |
| Mutability      | `write`                                                                 |
| Automatic retry | No automatic retries                                                    |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

JSON request body.

| Field          | Required | Type            | Constraints   | Description |
| -------------- | -------- | --------------- | ------------- | ----------- |
| `body.name`    | Yes      | `string`        | maxLength=100 | -           |
| `body.members` | No       | `Array<string>` | -             | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | Created successfully. Returns the new group ID.                            |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                           | Present | Type                | Constraints | Description                                                            |
| ------------------------------- | ------- | ------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`        | No      | `string`            | -           | The request ID generated by the server.                                |
| `result.data.client_request_id` | No      | `string`            | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`              | Yes     | `GroupCreateResult` | -           | -                                                                      |
| `result.data.data.id`           | Yes     | `string`            | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.createGroup({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  },
  body: {
    name: "Example"
  }
});
console.log(result.data.data);
```

---

<a id="get-group"></a>

## `getGroup`

Get group details

| Property        | Value                                                                |
| --------------- | -------------------------------------------------------------------- |
| HTTP            | `GET` `/v1/accounts/{account_id}/groups/info`                        |
| SDK method      | `device.getGroup(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["getGroup"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                 |
| Mutability      | `read`                                                               |
| Automatic retry | Safe retries enabled                                                 |

### Parameters

| SDK field                | Location | Required | Type     | Example       | Constraints | Description                                                                                                          |
| ------------------------ | -------- | -------- | -------- | ------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"`   | -           | The account `public_id` issued by the Device API, for example `acc_xxx`.                                             |
| `params.query.group_id`  | query    | Yes      | `string` | `"group_xxx"` | -           | The provider-side group ID. Because it may contain characters such as `@`, it is always passed as a query parameter. |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | Group details.                                                             |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                              | Present | Type                       | Constraints | Description                                                            |
| ---------------------------------- | ------- | -------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`           | No      | `string`                   | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`    | No      | `string`                   | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                 | Yes     | `Group`                    | -           | -                                                                      |
| `result.data.data.id`              | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.conversation_id` | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.name`            | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.avatar_url`      | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.member_count`    | No      | `number \| string (int64)` | -           | -                                                                      |
| `result.data.data.joined_at`       | No      | `string (date-time)`       | -           | -                                                                      |
| `result.data.data.created_at`      | No      | `string (date-time)`       | -           | -                                                                      |
| `result.data.data.description`     | No      | `string`                   | -           | -                                                                      |
| `result.data.data.extra`           | No      | `FreeFormObject`           | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.getGroup({
  params: {
    path: {
      account_id: "acc_xxx"
    },
    query: {
      group_id: "group_xxx"
    }
  }
});
console.log(result.data.data);
```

---

<a id="get-group-invite-code"></a>

## `getGroupInviteCode`

Get a group invitation link or code

| Property        | Value                                                                          |
| --------------- | ------------------------------------------------------------------------------ |
| HTTP            | `GET` `/v1/accounts/{account_id}/groups/invite-code`                           |
| SDK method      | `device.getGroupInviteCode(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["getGroupInviteCode"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                           |
| Mutability      | `read`                                                                         |
| Automatic retry | Safe retries enabled                                                           |

### Parameters

| SDK field                | Location | Required | Type     | Example       | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ------------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"`   | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |
| `params.query.group_id`  | query    | Yes      | `string` | `"group_xxx"` | -           | The provider-side group ID.                                              |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | Invitation details.                                                        |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                           | Present | Type              | Constraints | Description                                                            |
| ------------------------------- | ------- | ----------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`        | No      | `string`          | -           | The request ID generated by the server.                                |
| `result.data.client_request_id` | No      | `string`          | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`              | Yes     | `GroupInviteCode` | -           | -                                                                      |
| `result.data.data.invite_url`   | Yes     | `string`          | -           | -                                                                      |
| `result.data.data.invite_code`  | Yes     | `string`          | -           | -                                                                      |

> This operation may return one-time or sensitive data. Handle it only in controlled locations and never log the full response.

### TypeScript example

```ts
const result = await device.getGroupInviteCode({
  params: {
    path: {
      account_id: "acc_xxx"
    },
    query: {
      group_id: "group_xxx"
    }
  }
});
console.log(result.status, result.requestId);
```

---

<a id="leave-group"></a>

## `leaveGroup`

Leave a group

| Property        | Value                                                                  |
| --------------- | ---------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts/{account_id}/groups/leave`                        |
| SDK method      | `device.leaveGroup(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["leaveGroup"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                   |
| Mutability      | `destructive`                                                          |
| Automatic retry | No automatic retries                                                   |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

JSON request body.

| Field           | Required | Type     | Constraints | Description |
| --------------- | -------- | -------- | ----------- | ----------- |
| `body.group_id` | Yes      | `string` | -           | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The operation succeeded.                                                   |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                           | Present | Type      | Constraints | Description                                                            |
| ------------------------------- | ------- | --------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`        | No      | `string`  | -           | The request ID generated by the server.                                |
| `result.data.client_request_id` | No      | `string`  | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`              | Yes     | `OkData`  | -           | -                                                                      |
| `result.data.data.ok`           | Yes     | `boolean` | const=true  | -                                                                      |

### TypeScript example

```ts
const result = await device.leaveGroup({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  },
  body: {
    group_id: "group_xxx"
  }
});
console.log(result.data.data);
```

---

<a id="list-group-join-requests"></a>

## `listGroupJoinRequests`

List pending group join requests

| Property        | Value                                                                             |
| --------------- | --------------------------------------------------------------------------------- |
| HTTP            | `GET` `/v1/accounts/{account_id}/groups/join-requests`                            |
| SDK method      | `device.listGroupJoinRequests(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["listGroupJoinRequests"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                              |
| Mutability      | `read`                                                                            |
| Automatic retry | Safe retries enabled                                                              |

### Parameters

| SDK field                | Location | Required | Type     | Example       | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ------------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"`   | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |
| `params.query.group_id`  | query    | Yes      | `string` | `"group_xxx"` | -           | The provider-side group ID.                                              |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | A list of group join requests.                                             |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                   | Present | Type                      | Constraints | Description                                                            |
| --------------------------------------- | ------- | ------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`                | No      | `string`                  | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`         | No      | `string`                  | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                      | Yes     | `GroupJoinRequestsResult` | -           | -                                                                      |
| `result.data.data.group_id`             | Yes     | `string`                  | -           | -                                                                      |
| `result.data.data.items`                | Yes     | `Array<GroupJoinRequest>` | -           | -                                                                      |
| `result.data.data.items[].id`           | Yes     | `string`                  | -           | -                                                                      |
| `result.data.data.items[].phone`        | No      | `string`                  | -           | -                                                                      |
| `result.data.data.items[].lid`          | No      | `string`                  | -           | -                                                                      |
| `result.data.data.items[].requested_at` | No      | `string (date-time)`      | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.listGroupJoinRequests({
  params: {
    path: {
      account_id: "acc_xxx"
    },
    query: {
      group_id: "group_xxx"
    }
  }
});
console.log(result.data.data);
```

---

<a id="list-groups"></a>

## `listGroups`

List groups

| Property        | Value                                                                  |
| --------------- | ---------------------------------------------------------------------- |
| HTTP            | `GET` `/v1/accounts/{account_id}/groups`                               |
| SDK method      | `device.listGroups(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["listGroups"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                   |
| Mutability      | `read`                                                                 |
| Automatic retry | Safe retries enabled                                                   |

### Parameters

| SDK field                | Location | Required | Type      | Example        | Constraints | Description                                                                  |
| ------------------------ | -------- | -------- | --------- | -------------- | ----------- | ---------------------------------------------------------------------------- |
| `params.path.account_id` | path     | Yes      | `string`  | `"acc_xxx"`    | -           | The account `public_id` issued by the Device API, for example `acc_xxx`.     |
| `params.query.cursor`    | query    | No       | `string`  | `"cursor_xxx"` | -           | The `next_cursor` returned by the previous response.                         |
| `params.query.limit`     | query    | No       | `integer` | `1`            | minimum=1   | The number of items per page. The default may vary by provider when omitted. |

### Request body

No request body.

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | A paginated list of groups.                                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                                      | Present | Type                       | Constraints | Description                                                            |
| ------------------------------------------ | ------- | -------------------------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`                   | No      | `string`                   | -           | The request ID generated by the server.                                |
| `result.data.client_request_id`            | No      | `string`                   | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`                         | Yes     | `GroupListResult`          | -           | -                                                                      |
| `result.data.data.items`                   | Yes     | `Array<Group>`             | -           | -                                                                      |
| `result.data.data.items[].id`              | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.items[].conversation_id` | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.items[].name`            | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.items[].avatar_url`      | Yes     | `string`                   | -           | -                                                                      |
| `result.data.data.items[].member_count`    | No      | `number \| string (int64)` | -           | -                                                                      |
| `result.data.data.items[].joined_at`       | No      | `string (date-time)`       | -           | -                                                                      |
| `result.data.data.items[].created_at`      | No      | `string (date-time)`       | -           | -                                                                      |
| `result.data.data.items[].description`     | No      | `string`                   | -           | -                                                                      |
| `result.data.data.items[].extra`           | No      | `FreeFormObject`           | -           | -                                                                      |
| `result.data.data.next_cursor`             | No      | `string`                   | -           | -                                                                      |
| `result.data.data.has_more`                | Yes     | `boolean`                  | -           | -                                                                      |

### TypeScript example

```ts
const result = await device.listGroups({
  params: {
    path: {
      account_id: "acc_xxx"
    },
    query: {
      limit: 1
    }
  }
});
console.log(result.data.data);
```

---

<a id="set-group-join-approval-mode"></a>

## `setGroupJoinApprovalMode`

Set the group join approval mode

| Property        | Value                                                                                |
| --------------- | ------------------------------------------------------------------------------------ |
| HTTP            | `POST` `/v1/accounts/{account_id}/groups/join-approval-mode`                         |
| SDK method      | `device.setGroupJoinApprovalMode(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["setGroupJoinApprovalMode"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                                 |
| Mutability      | `write`                                                                              |
| Automatic retry | No automatic retries                                                                 |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

JSON request body.

| Field           | Required | Type      | Constraints | Description |
| --------------- | -------- | --------- | ----------- | ----------- |
| `body.group_id` | Yes      | `string`  | -           | -           |
| `body.enabled`  | Yes      | `boolean` | -           | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The operation succeeded.                                                   |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                           | Present | Type      | Constraints | Description                                                            |
| ------------------------------- | ------- | --------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`        | No      | `string`  | -           | The request ID generated by the server.                                |
| `result.data.client_request_id` | No      | `string`  | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`              | Yes     | `OkData`  | -           | -                                                                      |
| `result.data.data.ok`           | Yes     | `boolean` | const=true  | -                                                                      |

### TypeScript example

```ts
const result = await device.setGroupJoinApprovalMode({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  },
  body: {
    group_id: "group_xxx",
    enabled: false
  }
});
console.log(result.data.data);
```

---

<a id="update-group-info"></a>

## `updateGroupInfo`

Update group information

Only one of `name`, `description`, and `avatar_url` may be provided per request.

| Property        | Value                                                                       |
| --------------- | --------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts/{account_id}/groups/update-info`                       |
| SDK method      | `device.updateGroupInfo(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["updateGroupInfo"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                        |
| Mutability      | `write`                                                                     |
| Automatic retry | No automatic retries                                                        |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

JSON request body.

| Field              | Required | Type           | Constraints   | Description                                    |
| ------------------ | -------- | -------------- | ------------- | ---------------------------------------------- |
| `body.group_id`    | Yes      | `string`       | -             | -                                              |
| `body.name`        | No       | `string`       | maxLength=100 | -                                              |
| `body.description` | No       | `string`       | -             | Pass an empty string to clear the description. |
| `body.avatar_url`  | No       | `string (uri)` | -             | -                                              |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The operation succeeded.                                                   |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                           | Present | Type      | Constraints | Description                                                            |
| ------------------------------- | ------- | --------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`        | No      | `string`  | -           | The request ID generated by the server.                                |
| `result.data.client_request_id` | No      | `string`  | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`              | Yes     | `OkData`  | -           | -                                                                      |
| `result.data.data.ok`           | Yes     | `boolean` | const=true  | -                                                                      |

### TypeScript example

```ts
const result = await device.updateGroupInfo({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  },
  body: {
    group_id: "group_xxx",
    name: "Example group"
  }
});
console.log(result.data.data);
```

---

<a id="update-group-join-requests"></a>

## `updateGroupJoinRequests`

Process a group join request

| Property        | Value                                                                               |
| --------------- | ----------------------------------------------------------------------------------- |
| HTTP            | `POST` `/v1/accounts/{account_id}/groups/join-requests/update`                      |
| SDK method      | `device.updateGroupJoinRequests(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["updateGroupJoinRequests"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                                |
| Mutability      | `destructive`                                                                       |
| Automatic retry | No automatic retries                                                                |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

JSON request body.

| Field             | Required | Type                                 | Constraints              | Description |
| ----------------- | -------- | ------------------------------------ | ------------------------ | ----------- |
| `body.group_id`   | Yes      | `string`                             | -                        | -           |
| `body.action`     | Yes      | `string enum("approve" \| "reject")` | enum="approve", "reject" | -           |
| `body.member_ids` | Yes      | `Array<string>`                      | minItems=1               | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The operation succeeded.                                                   |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                           | Present | Type      | Constraints | Description                                                            |
| ------------------------------- | ------- | --------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`        | No      | `string`  | -           | The request ID generated by the server.                                |
| `result.data.client_request_id` | No      | `string`  | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`              | Yes     | `OkData`  | -           | -                                                                      |
| `result.data.data.ok`           | Yes     | `boolean` | const=true  | -                                                                      |

### TypeScript example

```ts
const result = await device.updateGroupJoinRequests({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  },
  body: {
    group_id: "group_xxx",
    action: "approve",
    member_ids: ["member_xxx"]
  }
});
console.log(result.data.data);
```

---

<a id="update-group-members"></a>

## `updateGroupMembers`

Manage group members

| Property        | Value                                                                          |
| --------------- | ------------------------------------------------------------------------------ |
| HTTP            | `POST` `/v1/accounts/{account_id}/groups/members`                              |
| SDK method      | `device.updateGroupMembers(request, execution?)`                               |
| SDK return type | `Promise<ApiResult<OperationData<DeviceApiOperations["updateGroupMembers"]>>>` |
| Authentication  | `X-Api-Key` (injected by the client)                                           |
| Mutability      | `destructive`                                                                  |
| Automatic retry | No automatic retries                                                           |

### Parameters

| SDK field                | Location | Required | Type     | Example     | Constraints | Description                                                              |
| ------------------------ | -------- | -------- | -------- | ----------- | ----------- | ------------------------------------------------------------------------ |
| `params.path.account_id` | path     | Yes      | `string` | `"acc_xxx"` | -           | The account `public_id` issued by the Device API, for example `acc_xxx`. |

### Request body

JSON request body.

| Field            | Required | Type                                                      | Constraints                               | Description |
| ---------------- | -------- | --------------------------------------------------------- | ----------------------------------------- | ----------- |
| `body.group_id`  | Yes      | `string`                                                  | -                                         | -           |
| `body.action`    | Yes      | `string enum("add" \| "remove" \| "promote" \| "demote")` | enum="add", "remove", "promote", "demote" | -           |
| `body.member_id` | Yes      | `string`                                                  | -                                         | -           |

### Responses

| HTTP status | Content-Type       | Schema                  | Description                                                                |
| ----------- | ------------------ | ----------------------- | -------------------------------------------------------------------------- |
| `200`       | `application/json` | `ResponseMeta & object` | The operation succeeded.                                                   |
| `400`       | `application/json` | `ErrorEnvelope`         | The request body or parameters are invalid.                                |
| `401`       | `application/json` | `ErrorEnvelope`         | The API key is missing or cannot be validated.                             |
| `404`       | `application/json` | `ErrorEnvelope`         | The resource or route does not exist.                                      |
| `409`       | `application/json` | `ErrorEnvelope`         | The request conflicts with the current state of the resource.              |
| `500`       | `application/json` | `ErrorEnvelope`         | An internal server error or an unmapped error in the provider integration. |
| `501`       | `application/json` | `ErrorEnvelope`         | The current provider does not support this capability.                     |
| `502`       | `application/json` | `ErrorEnvelope`         | This capability is unavailable for the current provider.                   |

The SDK returns `ApiResult<T>` with `data`, `status`, an optional `requestId`, and the underlying `response`.

Success status `200` response fields:

| Field                           | Present | Type      | Constraints | Description                                                            |
| ------------------------------- | ------- | --------- | ----------- | ---------------------------------------------------------------------- |
| `result.data.request_id`        | No      | `string`  | -           | The request ID generated by the server.                                |
| `result.data.client_request_id` | No      | `string`  | -           | Echoed when the client supplies a valid `X-Request-Id` request header. |
| `result.data.data`              | Yes     | `OkData`  | -           | -                                                                      |
| `result.data.data.ok`           | Yes     | `boolean` | const=true  | -                                                                      |

### TypeScript example

```ts
const result = await device.updateGroupMembers({
  params: {
    path: {
      account_id: "acc_xxx"
    }
  },
  body: {
    group_id: "group_xxx",
    action: "add",
    member_id: "member_xxx"
  }
});
console.log(result.data.data);
```
