/**
 * 此文件由 `pnpm generate` 根据 contracts/device.openapi.yaml 与 MCP package version 生成。
 * 生成边界用于保证契约、SDK 与 MCP 同步；请修改来源契约或生成器，不要手工编辑。
 */
export const generatedServerVersion = "0.1.0";

export const generatedCoverage = [
  {
    "api": "device",
    "sourceOperations": 64,
    "sdkMethods": 64,
    "mcpEligible": 4
  }
] as const;
